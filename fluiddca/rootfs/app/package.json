{
  "name": "fluid-dca-automation",
  "version": "2.0.0",
  "description": "Alchemy gas-sponsored automation scripts for FluidDCA contract",
  "main": "run-execution.ts",
  "scripts": {
    "check": "npx ts-node run-execution.ts --check-only",
    "dry-run": "npx ts-node run-execution.ts --dry-run",
    "execute": "npx ts-node run-execution.ts",
    "quote": "npx ts-node 1inch-quote.ts"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
